services:
  bot:
    build: .
    env_file: .env
    environment:
      DATABASE_PATH: /data/bot.db
      FEEDS_YAML_PATH: /app/feeds.yaml
    volumes:
      - bot_data:/data
    restart: unless-stopped

volumes:
  bot_data:
